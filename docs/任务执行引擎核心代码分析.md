# 任务执行引擎核心代码分析

> 本文档提取项目任务执行引擎的核心代码片段，供架构师分析调用链路和状态控制方式。

---

## 目录

1. [后台执行线程](#1-后台执行线程)
2. [任务控制器](#2-任务控制器)
3. [典型任务示例](#3-典型任务示例)
4. [调用链路总结](#4-调用链路总结)
5. [当前异常处理现状](#5-当前异常处理现状)

---

## 1. 后台执行线程

**文件路径**: `src/core/worker.py`

### 1.1 ScriptWorker 类定义

```python
class ScriptWorker(QThread):
    """
    后台脚本执行线程
    
    用于在后台执行用户选择的任务，避免阻塞UI线程。
    通过任务注册表动态获取任务函数，实现任务与执行器的解耦。
    
    属性：
        tasks: 用户选择的任务列表
        hwnd: 窗口句柄
        task_params: 任务参数字典
        
    信号：
        finished_sig: 任务完成信号
        task_completed: 单个任务完成信号 (task_name, result: bool | dict)
    """
    
    finished_sig = pyqtSignal()
    task_completed = pyqtSignal(str, object)

    def __init__(self, selected_tasks: List[str], hwnd: int, task_params: Optional[Dict[str, Any]] = None):
        super().__init__()
        self.tasks = selected_tasks
        self.hwnd = hwnd
        self.task_params = task_params or {}
        self.logger = get_logger('ScriptWorker')
```

### 1.2 run() 方法完整实现

```python
def run(self):
    """
    线程执行入口
    
    流程：
        1. 重置控制器状态
        2. 遍历执行选中的任务
        3. 捕获各类异常并处理
        4. 清理输入状态并发送完成信号
    
    异常隔离：
        - 每个任务执行都有独立的 try...except 块
        - 单任务崩溃不会导致整个线程意外退出
    """
    task_controller.reset_all_events()
    
    try:
        for task_name in self.tasks:
            try:
                # 检查窗口有效性
                if not win32gui.IsWindow(self.hwnd):
                    raise InvalidWindowHandleException("游戏窗口已关闭或无效")
                
                # 检查控制信号（暂停/停止）
                task_controller.check_status()
                
                # 通过注册表动态获取任务函数
                task_func = get_task(task_name)
                
                if task_func is not None:
                    params = self.task_params.get(task_name, {})
                    
                    result = None
                    # 特殊任务参数处理
                    if task_name == "摇钱树":
                        choice = params.get("choice", 1)
                        task_func(self.hwnd, choice=choice)
                    elif task_name == "每日可换":
                        result = task_func(self.hwnd, checkbox_state=params)
                    else:
                        result = task_func(self.hwnd)
                    
                    if result is None:
                        result = False
                    
                    self.task_completed.emit(task_name, result)
                else:
                    self.logger.info(f"任务 [{task_name}] 暂未编写逻辑实现")
                    time.sleep(1)
                    
            except TaskStoppedException:
                self.logger.warning(f"任务 [{task_name}] 被用户中止")
                self.task_completed.emit(task_name, False)
                raise
                
            except ContextExpiredException:
                self.logger.warning(f"任务 [{task_name}] 上下文失效，尝试重置游戏状态")
                reset_game_state(self.hwnd)
                
            except InvalidWindowHandleException as e:
                self.logger.error(f"{str(e)}，请重新绑定窗口")
                break
                
            except Exception as e:
                self.logger.error(f"任务 [{task_name}] 执行异常: {str(e)}")
                self.task_completed.emit(task_name, False)
        
        self.logger.info("当前任务已全部完成")
        
    except TaskStoppedException:
        self.logger.warning("任务被用户中止")
        
    except Exception as e:
        self.logger.error(f"任务执行错误: {str(e)}")
        
    finally:
        release_tracked_inputs(self.hwnd)
        self.finished_sig.emit()
```

### 1.3 关键调用链路

| 步骤 | 操作 | 说明 |
|------|------|------|
| 1 | `task_controller.reset_all_events()` | 重置所有控制信号 |
| 2 | `task_controller.check_status()` | 每个任务前检查暂停/停止 |
| 3 | `get_task(task_name)` | 从注册表获取任务函数 |
| 4 | `task_func(self.hwnd, ...)` | 执行任务函数 |
| 5 | `task_completed.emit()` | 发送任务完成信号 |
| 6 | `release_tracked_inputs()` | 清理输入状态 |
| 7 | `finished_sig.emit()` | 发送全部完成信号 |

---

## 2. 任务控制器

**文件路径**: `src/core/controller.py`

### 2.1 异常类定义

```python
class TaskStoppedException(Exception):
    """
    任务被用户强制停止异常
    
    当用户点击停止按钮或调用 stop() 方法后，
    任务执行过程中检测到停止信号时抛出此异常
    """
    pass


class ContextExpiredException(Exception):
    """
    上下文过期异常
    
    当任务暂停时间超过阈值，导致游戏状态可能发生变化，
    继续执行可能产生不可预期结果时抛出此异常
    """
    pass


class InvalidWindowHandleException(Exception):
    """
    窗口句柄无效异常
    
    当目标窗口句柄无效或窗口已关闭时抛出此异常
    """
    pass
```

### 2.2 TaskController 类定义

```python
class TaskController:
    """
    任务控制器单例类
    
    用于控制任务的执行状态，支持暂停、继续、停止操作。
    使用 threading.Event 实现线程间的同步控制。
    
    属性：
        _instance: 单例实例
        _pause_event: 暂停事件（True=放行，False=阻塞）
        _stop_event: 停止事件（True=触发停止）
        _pause_start_time: 暂停开始时间戳
        _custom_timeout: 任务自定义超时阈值（秒）
    """
    
    _instance: Optional['TaskController'] = None
    _lock: threading.Lock = threading.Lock()
```

### 2.3 check_status() 方法

```python
def check_status(self, timeout: Optional[float] = None) -> None:
    """
    检查任务状态
    
    先检查是否收到停止信号，如果已停止则抛出 TaskStoppedException。
    然后等待暂停事件，如果任务被暂停则阻塞直到恢复。
    
    参数：
        timeout: 等待暂停事件的最大时间（秒），
                None 表示无限等待
                0 表示立即返回（仅检测，不等待）
    
    异常：
        TaskStoppedException: 当任务被停止时抛出
    """
    if self._stop_event.is_set():
        raise TaskStoppedException("任务已被用户停止")
    
    if timeout == 0:
        return
    
    if not self._pause_event.is_set():
        if timeout is not None:
            self._pause_event.wait(timeout=timeout)
        else:
            self._pause_event.wait()
        
        if self._stop_event.is_set():
            raise TaskStoppedException("任务已被用户停止")
```

### 2.4 smart_sleep() 方法

```python
def smart_sleep(self, seconds: float) -> None:
    """
    智能睡眠函数
    
    将睡眠时间切分为 100ms 时间片，每个时间片结束后检查控制状态，
    实现可中断的睡眠。使用操作系统级 time.sleep 让出 CPU，避免忙等待。
    
    设计原理：
        1. 时间片切分：将长时间睡眠切分为 100ms 的小片段
        2. CPU 让出：每个时间片调用 time.sleep(0.1) 让出 CPU
        3. 状态检查：每个时间片后调用 check_status() 检查暂停/停止
        4. 精度补偿：动态计算剩余时间，避免累积误差
    
    参数：
        seconds: 睡眠时长（秒），支持浮点数
    
    异常：
        TaskStoppedException: 当睡眠期间收到停止信号时抛出
    """
    if seconds <= 0:
        return
    
    start_time = time.time()
    end_time = start_time + seconds
    time_slice = 0.1  # 100ms 时间片
    
    while True:
        current_time = time.time()
        remaining = end_time - current_time
        
        if remaining <= 0:
            break
        
        sleep_duration = min(time_slice, remaining)
        
        time.sleep(sleep_duration)
        
        self.check_status()
```

### 2.5 pause() / resume() / stop() 方法

```python
def pause(self, timeout: Optional[float] = None) -> None:
    """
    暂停任务执行
    
    清除暂停事件，使任务在调用 check_status() 时阻塞。
    """
    self._pause_start_time = time.time()
    self._custom_timeout = timeout
    self._pause_event.clear()


def resume(self) -> None:
    """
    恢复任务执行
    
    设置暂停事件，使阻塞的任务继续执行。
    如果暂停时间超过超时阈值，抛出 ContextExpiredException。
    """
    if self._pause_start_time is not None:
        pause_duration = time.time() - self._pause_start_time
        timeout_threshold = self._custom_timeout or config.pause_timeout_threshold
        
        if pause_duration > timeout_threshold:
            self._pause_event.set()
            self._pause_start_time = None
            self._custom_timeout = None
            raise ContextExpiredException(
                f"暂停时间 {pause_duration:.1f}秒 超过阈值 {timeout_threshold:.1f}秒，上下文已过期"
            )
    
    self._pause_event.set()
    self._pause_start_time = None
    self._custom_timeout = None


def stop(self) -> None:
    """
    停止任务执行
    
    同时设置停止事件和暂停事件。
    设置暂停事件是为了唤醒正在暂停等待中的线程。
    """
    self._stop_event.set()
    self._pause_event.set()
```

### 2.6 状态控制机制

```
┌─────────────────────────────────────────────────────────────┐
│                    状态控制流程                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  UI 层调用           控制器状态变更          任务线程响应     │
│  ─────────          ─────────────          ─────────────    │
│                                                             │
│  pause()    ──►    _pause_event.clear()  ──►  阻塞等待      │
│                                                             │
│  resume()   ──►    _pause_event.set()    ──►  继续执行      │
│                      (超时则抛 ContextExpiredException)      │
│                                                             │
│  stop()     ──►    _stop_event.set()     ──►  抛出          │
│                     _pause_event.set()         TaskStopped-  │
│                                                 Exception   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 3. 典型任务示例

**文件路径**: `src/core/daily_tasks.py`

### 3.1 task_gua() 函数完整实现

```python
@register_task("每日一卦")
def task_gua(hwnd, log_signal=None):
    """
    每日一卦任务
    
    参数：
        hwnd: 窗口句柄
        log_signal: 日志信号对象（可选，已弃用）
    """
    logger.info("正在处理: 每日一卦")
    
    # 整个任务的最大允许重试次数，防止因特殊情况（如断线、弹窗卡死）陷入无限循环
    max_task_attempts = 6

    for attempt in range(1, max_task_attempts + 1):
        if attempt > 1:
            logger.debug(f"每日一挂：第 {attempt} 次重新尝试")

        # 1. 准备阶段
        zhd(hwnd)
        background_click(hwnd, 521, 476, button="left", delay=60)
        task_controller.smart_sleep(1)

        # 2. 检查任务入口
        pos = find_image(hwnd, config.get_img_path("richang_/mryg.png"), threshold=0.8)
        if pos is None:
            # 没找到任务入口，说明任务可能已经做完了
            logger.success("每日一挂已完成 (未找到任务入口)")
            win_gb(hwnd)
            return True  # 任务完成，直接退出函数
        
        # 找到任务入口，点击接取
        background_click(hwnd, 319, 290, button="left", delay=60)

        # 3. 寻路阶段 (加入最大等待次数防卡死，10秒 * 6次 = 最多等 60 秒)
        max_find_attempts = 6
        npc_found = False
        
        for i in range(max_find_attempts):
            pos_smbg = find_image(hwnd, config.get_img_path("richang_/smbg.png"), threshold=0.8)
            if pos_smbg is None:
                logger.debug(f"每日一挂寻路中... ({i + 1}/{max_find_attempts})")
                task_controller.smart_sleep(10)
            else:
                # 找到 NPC，双击交互
                background_click(hwnd, pos_smbg[0], pos_smbg[1], button="left", delay=60)
                task_controller.smart_sleep(1)
                background_click(hwnd, pos_smbg[0], pos_smbg[1], button="left", delay=60)
                npc_found = True
                break  # 成功找到，跳出寻路循环

        # 如果 60 秒都没走到，进行重试
        if not npc_found:
            logger.warning("寻路超时，未能找到算命卜卦NPC，正在重置任务流程...")
            win_gb(hwnd)
            continue  # 结束当前轮次，回到 for 循环开头重新 zhd(hwnd)

        task_controller.smart_sleep(3)

        # 4. 卜卦操作阶段 - 听天由命
        pos_ttym = find_image(hwnd, config.get_img_path("richang_/ttym.png"), threshold=0.8)
        if pos_ttym is None:
            logger.warning("未能找到[听天由命]按钮，正在重置任务流程...")
            win_gb(hwnd)
            continue

        background_click(hwnd, pos_ttym[0], pos_ttym[1], button="left", delay=60)
        task_controller.smart_sleep(3)
        background_click(hwnd, 730, 398, button="left", delay=60)  # 绝对坐标点击
        task_controller.smart_sleep(10)  # 等待动画或结算

        # 5. 领奖阶段 - 接受卦象
        pos_jsgx = find_image(hwnd, config.get_img_path("richang_/jsgx.png"), threshold=0.8)
        if pos_jsgx is None:
            logger.warning("未能找到[接受卦象]按钮，正在重置任务流程...")
            win_gb(hwnd)
            continue

        background_click(hwnd, pos_jsgx[0], pos_jsgx[1], button="left", delay=60)
        task_controller.smart_sleep(3)
        background_click(hwnd, 626, 355, button="left", delay=60)  # 绝对坐标点击
        task_controller.smart_sleep(2)
        
        # 6. 任务彻底完成，清理并退出
        wp_gb(hwnd)
        logger.success("每日一挂已完成")
        return True    # 安全退出函数

    # 如果 for 循环跑完了都没遇到 return，说明3次尝试都失败了
    logger.error("每日一挂任务执行失败，已达到最大重试次数，跳过该任务")
    win_gb(hwnd)
    return False
```

### 3.2 任务函数签名分析

| 参数 | 类型 | 说明 |
|------|------|------|
| `hwnd` | `int` | 窗口句柄（必传） |
| `log_signal` | `Optional` | 日志信号对象（已弃用，保留兼容） |

### 3.3 返回值约定

| 返回值 | 含义 |
|--------|------|
| `True` | 任务成功完成 |
| `False` | 任务执行失败 |
| `None` | 被视为 `False`（在 worker 中处理） |

### 3.4 缓存机制体现

`find_image()` 函数内部已集成双重缓存：

| 缓存类型 | 实现类 | 特性 |
|----------|--------|------|
| 帧缓存 | `FrameCache` | TTL 100ms，避免重复截图 |
| 模板缓存 | `TemplateCache` | 懒加载，避免重复磁盘 I/O |

---

## 4. 调用链路总结

```
┌─────────────────────────────────────────────────────────────┐
│                    完整调用链路                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  UI 层                                                      │
│    │                                                        │
│    ▼                                                        │
│  ScriptWorker.run()                                         │
│    │                                                        │
│    ├── task_controller.reset_all_events()                   │
│    │                                                        │
│    └── for task_name in tasks:                              │
│          │                                                  │
│          ├── task_controller.check_status()  ◄── 暂停/停止  │
│          │                                                  │
│          ├── get_task(task_name)                            │
│          │                                                  │
│          └── task_func(hwnd, ...)                           │
│                │                                            │
│                ├── find_image()                             │
│                │     ├── FrameCache.get_frame()  ◄── 帧缓存 │
│                │     └── TemplateCache.get()     ◄── 模板缓存│
│                │                                            │
│                ├── background_click()                       │
│                │     └── FrameCache.clear()     ◄── 清除缓存│
│                │                                            │
│                └── task_controller.smart_sleep() ◄── 可中断 │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 5. 当前异常处理现状

### 5.1 异常类型与处理方式

| 异常类型 | 触发场景 | 当前处理方式 |
|----------|----------|--------------|
| `TaskStoppedException` | 用户点击停止 | 记录日志，发射 `task_completed(False)`，终止线程 |
| `ContextExpiredException` | 暂停超时 | 重置游戏状态，继续下一个任务 |
| `InvalidWindowHandleException` | 窗口关闭 | 记录错误，终止线程 |
| `Exception` | 其他异常 | 记录错误，发射 `task_completed(False)`，继续下一个任务 |

### 5.2 异常处理流程图

```
┌─────────────────────────────────────────────────────────────┐
│                    异常处理流程                              │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  任务执行                                                    │
│    │                                                        │
│    ▼                                                        │
│  ┌─────────────────────────────────────┐                    │
│  │ try:                                │                    │
│  │   task_controller.check_status()    │                    │
│  │   task_func(hwnd, ...)              │                    │
│  │   task_completed.emit(True)         │                    │
│  └─────────────────────────────────────┘                    │
│    │                                                        │
│    ├── TaskStoppedException ──► emit(False) + raise         │
│    │                                                        │
│    ├── ContextExpiredException ──► reset_game_state()       │
│    │                                                        │
│    ├── InvalidWindowHandleException ──► break               │
│    │                                                        │
│    └── Exception ──► emit(False) + continue                 │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 5.3 待优化点（供架构师参考）

| 问题 | 现状 | 建议优化方向 |
|------|------|--------------|
| **无自动重试** | 任务异常后直接标记失败继续下一个 | 引入任务级重试机制 |
| **异常信息丢失** | 仅记录 `str(e)`，无堆栈信息 | 增加 `exc_info=True` 记录完整堆栈 |
| **无自愈逻辑** | 异常后无状态恢复尝试 | 引入游戏状态检测与自动恢复 |
| **重试策略单一** | 仅 `task_gua` 有内置重试逻辑 | 统一抽取为装饰器或基类方法 |

---

*文档生成时间: 2026-03-09*
