在保持当前所有功能完整不变的前提下，对"中部配置-日常任务"页面区域进行布局重构与功能实现。具体要求如下：

1. 页面布局调整：
   - 将现有"中部配置-日常任务"页面区域平均分割为左右两部分，左侧宽度占比为固定px的1/3，右侧宽度占比为固定px的2/3，计算划分为固定值
   
2. 左侧区域实现：
   - 创建一个支持垂直滚动的视图容器组件
   - 在滚动视图内实现两排复选框布局
   - 将系统中已有的复选框任务完整迁移至左侧滚动视图中
   - 预留后续添加新任务复选框的扩展接口，确保新添加的任务能正确显示在滚动视图内
   
3. 右侧区域实现：
   - 创建一个支持垂直滚动的视图容器组件
   - 该区域用于展示具体的任务配置内容（包含复选框、下拉框、文字框等交互元素），所有配置内容都在视图容器组件内，滚动可全部浏览
   - 实现左侧复选框与右侧配置内容的联动机制：当勾选左侧特定任务复选框时，右侧区域滚动到该任务对应的配置内容
   - 举例：例如“摇钱树”的“choice”参数就需要在右侧区域设置一个下拉框，默认为“轻轻摇”，下拉列表有“轻轻摇”、“用力摇”和“全力摇”，读取用户选择的配置通过并映射替换为0，1，2，传入函数，实现山河器任务的点击
   - 右侧需要对不同任务的配置信息进行垂直的区域划分，宽度占满
   - 预留后续添加新任务配置内容的扩展接口，确保新添加的任务配置内容能正确显示在滚动视图内

4. 交互与状态管理：
   - 确保左侧复选框的勾选状态能够被正确记录和持久化
   - 实现右侧配置内容的动态加载与切换逻辑
   - 初始化为全部复选框默认不勾选，其他内容也为默认状态，用户设置配置了状态，可保存到本地，下次登录默认读取用户配置,同时可让用户切换任务配置，如用户没有配置就使用初始化配置
   - 保持原有功能的完整性和正确性，包括但不限于任务保存、配置生效等核心功能

5. 视觉与用户体验：
   - 确保滚动视图的滚动条样式统一且美观
   - 实现平滑的视图切换过渡效果
   - 保证所有交互元素的可访问性和操作反馈

请基于上述需求进行修改，确保代码结构清晰、注释完善，并与现有系统架构保持一致。